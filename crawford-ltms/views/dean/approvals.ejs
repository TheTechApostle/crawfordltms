<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-4 pb-3 border-bottom">
  <div><div class="kicker">Dean / Timetable Committee<% if (faculty) { %> · <%= faculty.name %><% } %></div><h1 class="h3 mb-0">Approval queue</h1></div>
  <% if (session) { %><span class="badge bg-light text-dark border"><%= session.name %></span><% } %>
</div>

<div class="card"><div class="card-body">
  <p class="text-secondary">Final approval across all departments in the faculty. Cross-check shared venues and electives before signing off — the registrar publishes once you approve.</p>
  <% if (!faculty) { %>
    <p class="text-secondary mb-0">No faculty assigned to your account yet — ask the registrar to set you as Dean of a faculty.</p>
  <% } else if (!entries.length) { %>
    <p class="text-secondary mb-0">Nothing awaiting final approval right now.</p>
  <% } else { %>
    <div class="table-responsive"><table class="table table-hover align-middle mb-0">
      <thead><tr><th>Course</th><th>Dept</th><th>Lecturer</th><th>Venue</th><th>Slot</th><th></th></tr></thead>
      <tbody>
      <% entries.forEach(function(e){ %>
        <tr>
          <td><%= e.code %> — <%= e.title %></td>
          <td><%= e.dept_name %></td>
          <td><%= e.lecturer_name || 'Unassigned' %></td>
          <td><%= e.venue_name %></td>
          <td><%= e.day %> <%= e.start_time %>–<%= e.end_time %></td>
          <td class="d-flex gap-2">
            <form method="POST" action="/dean/approvals/<%= e.id %>/approve"><button class="btn btn-sm btn-gold" type="submit"><i class="fa-solid fa-check me-1"></i> Approve</button></form>
            <form method="POST" action="/dean/approvals/<%= e.id %>/reject">
              <input type="hidden" name="reason" value="Cross-department clash">
              <button class="btn btn-sm btn-danger" type="submit"><i class="fa-solid fa-xmark me-1"></i> Reject</button>
            </form>
          </td>
        </tr>
      <% }) %>
      </tbody>
    </table></div>
  <% } %>
</div></div>
